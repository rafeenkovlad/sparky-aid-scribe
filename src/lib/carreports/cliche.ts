// Prompt templates passed to AiQueue.ChatCompletions as the `cliche` param.
// Текст эксперта приходит отдельным полем `text` — в самом клише не нужно
// дублировать его плейсхолдером {text}.
// Keep prompts short, demand strict JSON.

const COMMON = `Ты — ассистент технического осмотра автомобиля. Извлекай факты
строго из текста эксперта ниже. Не выдумывай. Отвечай ТОЛЬКО валидным JSON
без пояснений и без markdown-обрамления. Если поле не упомянуто — не
включай его в JSON.`;

export const CLICHE_CAR = `${COMMON}

Извлеки данные шага «Автомобиль». Поля:
- vin: строка из 17 символов A-Z/0-9 (если упомянут).
- gosNumber: госномер.
- uriListing: ссылка на объявление (URL).
- mileage: пробег в км, целое.
- cityInspection: город осмотра.
- dateInspection: YYYY-MM-DD, если эксперт назвал дату.
- unreadableVin: true если эксперт сказал, что VIN не читается.
- visuallyMileageNotMatchCondition: true если пробег не соответствует состоянию.
- brandName: марка автомобиля, нормализованная (например "VW"/"вольксваген" → "Volkswagen").
- modelCarName: модель (например "Tiguan", "Camry").
- year: год выпуска (целое 4 цифры), если назван.
- generationHint: поколение/модификация как назвал эксперт
  (например "2 поколение", "II", "FL", "рестайлинг", "MQB", "B8.5"). Любая строка.


`;

export const CLICHE_CHARACTERISTICS = `${COMMON}

Извлеки характеристики автомобиля. Поля:
- brandName, modelCarName, year (целое 4 цифры).
- generationHint: подсказка о поколении/модификации, как её назвал эксперт
  (например "2 поколение", "II", "FL", "рестайлинг", "MQB", "B8.5"). Любая строка.
- engineVolume (литры, число), enginePower (л.с., целое).
- engineType: одно из ["Бензин","Дизель","Гибрид","Электро","Газ/Бензин"].
- transmission: одно из ["АКПП","МКПП","Робот","Вариатор"].
- driveType: одно из ["Передний","Задний","Полный"].
- color, equipment (название комплектации).


`;

/**
 * Подбор справочника карreports: бренд → модель → поколение/рестайлинг.
 * Сервер отдаёт реальные списки методами Storage.GetBrand / Storage.GetModelCar /
 * Storage.GetModelGeneration, а ИИ выбирает ОДИН вариант из списка.
 */
export const CLICHE_PICK_BRAND = (
  userText: string,
  hint: string | undefined,
  brands: Array<{ id: number; name: string; country?: string | null }>,
  webContext?: string,
) => `${COMMON}

Тебе дан список брендов автомобилей из каталога carreports
(метод Storage.GetBrand). Выбери ОДИН id, который лучше всего подходит
к подсказке эксперта. Не придумывай свой id, только из списка.

Подсказка эксперта по бренду: ${JSON.stringify(hint ?? "")}
Исходный текст эксперта: ${JSON.stringify(userText)}
${webContext ? `\nКонтекст из веб-поиска (используй для нормализации сокращений вроде «VW» → «Volkswagen»):\n${webContext}\n` : ""}
Кандидаты (id — name [country]):
${brands.slice(0, 80).map((b) => `  • ${b.id} — ${b.name}${b.country ? ` [${b.country}]` : ""}`).join("\n") || "  (пусто)"}

Верни ТОЛЬКО JSON:
{
  "brandId": <число из списка или null>,
  "confidence": <0..1>,
  "needsWeb": <true если уверенность низкая и стоит уточнить веб-поиском>,
  "reason": "короткое пояснение"
}


`;


export const CLICHE_PICK_MODEL = (
  userText: string,
  brandName: string,
  modelHint: string | undefined,
  models: Array<{ id: number; name: string; aliases?: string[] }>,
  webContext?: string,
) => `${COMMON}

Тебе дан список моделей бренда «${brandName}» из каталога carreports
(метод Storage.GetModelCar по brandId). Выбери ОДИН id, который лучше
всего соответствует ПОДСКАЗКЕ ПО МОДЕЛИ. Только из списка.

КРИТИЧНО:
- Подсказка по модели — ГЛАВНЫЙ сигнал. Сравнивай с полем name кандидатов.
- Цифры/слова вроде «2», «II», «поколение», «рестайлинг», «FL», «MQB»,
  «дизель», «бензин», «АКПП», год — это НЕ часть имени модели. Игнорируй их
  при подборе модели; они относятся к поколению, типу двигателя и т.п.
- Если в подсказке есть слово, которое точно совпадает с одним из name из
  списка (например «Tiguan» → «Tiguan»), выбирай именно его, даже если рядом
  есть другие цифры/слова.

Подсказка эксперта по модели: ${JSON.stringify(modelHint ?? "")}
Исходный текст эксперта: ${JSON.stringify(userText)}
${webContext ? `\nКонтекст из веб-поиска (используй чтобы понять, какая модель имеется в виду):\n${webContext}\n` : ""}
Кандидаты (id — name / aliases):
${
  models
    .slice(0, 120)
    .map((m) => {
      const aliases = (m.aliases ?? []).filter((a) => a && a !== m.name);
      return `  • ${m.id} — ${m.name}${aliases.length ? ` / ${aliases.join(", ")}` : ""}`;
    })
    .join("\n") || "  (пусто)"
}

Верни ТОЛЬКО JSON:
{
  "modelCarId": <число из списка или null>,
  "confidence": <0..1>,
  "needsWeb": <true если стоит уточнить веб-поиском>,
  "reason": "короткое пояснение"
}


`;


export interface GenerationFrameCandidate {
  frameId: number;
  generationName?: string;
  restylingName?: string;
  /** numeric generation index from API (`generation` field) */
  generationNumber?: number;
  /** numeric restyling index from API (`restyling` field), 0 = базовый */
  restylingNumber?: number;
  yearStart?: number | null;
  yearEnd?: number | null;
  urlImage?: string;
}

export const CLICHE_PICK_GENERATION = (
  userText: string,
  brandName: string,
  modelName: string,
  year: number | undefined,
  generationHint: string | undefined,
  frames: GenerationFrameCandidate[],
  webContext?: string,
) => `${COMMON}

Тебе дан плоский список рестайлинг-фреймов модели «${brandName} ${modelName}»
из каталога carreports (метод Storage.GetModelGeneration по modelCarId,
раскрытый по рестайлингам и фреймам). Выбери ОДИН frameId, который лучше
всего подходит по году и подсказке эксперта. Только из списка.

Год выпуска авто: ${year ?? "не указан"}
Подсказка по поколению/рестайлингу: ${JSON.stringify(generationHint ?? "")}
Исходный текст эксперта: ${JSON.stringify(userText)}
${webContext ? `\nКонтекст из веб-поиска (годы выпуска поколений, кодовые имена и т.п.):\n${webContext}\n` : ""}


Кандидаты (frameId — поколение / рестайлинг / годы):
${
  frames
    .slice(0, 60)
    .map(
      (f) =>
        `  • ${f.frameId} — ${f.generationName ?? "?"}${
          f.restylingName ? ` / ${f.restylingName}` : ""
        } [${f.yearStart ?? "?"}–${f.yearEnd ?? "н.в."}]`,
    )
    .join("\n") || "  (пусто)"
}

Верни ТОЛЬКО JSON:
{
  "frameId": <число из списка или null>,
  "confidence": <0..1>,
  "needsWeb": <true если стоит уточнить веб-поиском>,
  "reason": "короткое пояснение"
}


`;

export const CLICHE_DOCS = `${COMMON}

Извлеки данные сверки документов. Поля:
- ownersCount: целое (число владельцев по ПТС).
- ownerFullNameMatchWithPTSOrSTS: true/false (собственник в ПТС/СТС совпадает с продавцом).
- vinOnBodyMatchWithPTSOrSTS: true/false (VIN на кузове совпадает с документами).
- engineModelMatchWithPTSOrSTS: true/false (номер двигателя совпадает с ПТС).
- note: краткая заметка про расхождения, если есть.


`;

export const CLICHE_INSPECTION = (
  zoneLabel: string,
  sectionLabel: string,
  elements: Array<{ id: string; label: string }>,
  knownTags: Array<{ name: string; type?: string | null }>,
) => {
  const elList = elements.map((el) => `  • ${el.id} — ${el.label}`).join("\n");
  const tagList = knownTags.length
    ? knownTags
        .slice(0, 60)
        .map(
          (t) => `  • ${t.name}${t.type ? ` [${t.type === "serious" ? "серьёзный" : "не серьёзный"}]` : ""}`,
        )
        .join("\n")
    : "  (каталог пуст — извлекай теги свободным текстом)";

  return `${COMMON}

Это заметка эксперта по зоне «${zoneLabel}» (серверная секция «${sectionLabel}»).
Раздели её на находки по конкретным элементам секции. Для каждого упомянутого
элемента укажи: дефекты есть или нет, какие теги повреждений (серьёзные —
сквозная коррозия, перекрас, замена силового элемента, ДТП; не серьёзные —
сколы, царапины, локальные подкрасы, потёртости), и краткую техническую
заметку.

ВАЖНО про серьёзность: если эксперт ЯВНО назвал дефект серьёзным
(слова «серьёзная», «серьезная», «сильная», «глубокая», «большая»,
«крупная», «значительная») — этот тег обязательно идёт в seriousTags,
даже если по умолчанию (вмятина, царапина и т.п.) он был бы не серьёзным.
И наоборот: «мелкая», «маленькая», «поверхностная», «лёгкая» — в nonSeriousTags.
Слова-маркеры серьёзности НЕ включай в имя тега — имя оставляй коротким
(например «Вмятина», а не «Серьёзная вмятина»).

Элементы секции (используй ИМЕННО эти id):
${elList}

Каталог тегов секции (приоритет — выбирать из него; если не подходит — придумай короткое название):
${tagList}

Верни ТОЛЬКО валидный JSON:
{
  "note": "очищенная общая заметка по зоне (1–3 предложения)",
  "findings": [
    {
      "elementId": "<id из списка выше>",
      "noDamage": true|false,
      "seriousTags": ["имя_тега", ...],     // только серьёзные дефекты
      "nonSeriousTags": ["имя_тега", ...],  // мелкие дефекты
      "note": "краткая заметка по элементу"
    }
  ]
}
Если элемент явно не упомянут — не добавляй его. Если эксперт сказал по зоне
в общем («кузов в норме», «салон без замечаний») — добавь одну находку с
elementId="generalCondition" и noDamage=true.


`;
};

/** Клише для распознавания одного фото осмотра. */
export const CLICHE_INSPECTION_PHOTO = (
  sectionLabel: string,
  elements: Array<{ id: string; label: string; hint?: string }>,
  knownTags: Array<{ name: string; type?: string | null }>,
  existingNote?: string,
) => {
  const elList = elements
    .map((el) => {
      const h = el.hint?.trim();
      return h
        ? `  • ${el.id} — ${el.label}\n      ↳ ${h}`
        : `  • ${el.id} — ${el.label}`;
    })
    .join("\n");
  const tagList = knownTags.length
    ? knownTags
        .slice(0, 60)
        .map(
          (t) => `  • ${t.name}${t.type ? ` [${t.type === "serious" ? "серьёзный" : "не серьёзный"}]` : ""}`,
        )
        .join("\n")
    : "  (каталог пуст — придумай короткие имена сам)";

  return `${COMMON}

На фото — элемент автомобиля из раздела «${sectionLabel}». Определи:
1) Какой именно элемент из списка изображён (один elementId). Используй
   подсказки-референсы под каждым id — они описывают характерные признаки
   (расположение, форма, что вокруг). Сопоставь видимое на фото с этими
   признаками и выбери НАИБОЛЕЕ подходящий id. Если фото общее (несколько
   элементов сразу) или не удаётся однозначно определить — используй
   "generalCondition". ВАЖНО: если в заметке эксперта перечислено
   несколько разных элементов (например «царапина на капоте и крыле,
   скол на двери»), это заметка по зоне в целом — ставь
   elementId="generalCondition", не пытайся выбрать один из них.
2) Видны ли дефекты. Серьёзные: коррозия сквозная, перекрас, замена силового
   элемента, следы ДТП, треснутый элемент. Не серьёзные: сколы, царапины,
   локальные подкрасы, потёртости, грязь.
   ВАЖНО: если эксперт в тексте ЯВНО назвал дефект серьёзным
   («серьёзная/серьезная», «сильная», «глубокая», «большая», «крупная»,
   «значительная») — относи тег в seriousTags независимо от типа дефекта.
   «Мелкая», «маленькая», «поверхностная», «лёгкая» — в nonSeriousTags.
   Слова-маркеры серьёзности в имя тега НЕ включай (тег — «Вмятина», а не
   «Серьёзная вмятина»).
3) Подбери теги из каталога (приоритет — точное совпадение по смыслу). Если
   подходящего тега нет — придумай короткое имя (1–3 слова).
4) Сформулируй заметку 1–2 предложения по делу: что видно, где, насколько критично.

Элементы раздела (используй ИМЕННО эти id; референс — после стрелки ↳):
${elList}

Каталог тегов раздела:
${tagList}

Верни ТОЛЬКО валидный JSON одного объекта:
{
  "elementId": "<id из списка>",
  "noDamage": true|false,
  "seriousTags": ["имя_тега", ...],
  "nonSeriousTags": ["имя_тега", ...],
  "note": "что видно на фото"
}
Если дефектов нет — noDamage=true, оба массива тегов пустые.
${existingNote?.trim() ? `\nВАЖНО: к этому элементу уже есть СОХРАНЁННАЯ заметка эксперта:\n"""\n${existingNote.trim()}\n"""\nНовая заметка эксперта — это ДОПОЛНЕНИЕ, а не замена. В поле note верни ОБЪЕДИНЁННУЮ заметку: сохрани все факты из старой и добавь новые. Не выбрасывай детали из старой заметки. Если старая и новая описывают одно и то же — объедини без повторов.\n` : ""}
Дополнительный контекст эксперта (может быть пустым):
`;
};

// Text-only вариант (без фото) — обрабатываем заметку эксперта.
export const CLICHE_INSPECTION_NOTE = (
  sectionLabel: string,
  elements: Array<{ id: string; label: string; hint?: string }>,
  currentElementId: string | null,
  knownTags: Array<{ name: string; type?: string | null }>,
  existingNote?: string,
) => {
  const elList = elements
    .map((el) => {
      const mark = el.id === currentElementId ? " ← текущий выбранный" : "";
      const h = el.hint?.trim();
      return h
        ? `  • ${el.id} — ${el.label}${mark}\n      ↳ ${h}`
        : `  • ${el.id} — ${el.label}${mark}`;
    })
    .join("\n");
  const tagList = knownTags.length
    ? knownTags
        .slice(0, 60)
        .map(
          (t) => `  • ${t.name}${t.type ? ` [${t.type === "serious" ? "серьёзный" : "не серьёзный"}]` : ""}`,
        )
        .join("\n")
    : "  (каталог пуст — придумай короткие имена сам)";
  return `Ты — ассистент технического осмотра автомобиля. Анализируй заметку эксперта по разделу осмотра. Отвечай ТОЛЬКО валидным JSON одного объекта без markdown, без пояснений и без любого текста вокруг.

Раздел осмотра: «${sectionLabel}».
Эксперт надиктовал заметку текстом (БЕЗ фото). Заметка приходит отдельным сообщением пользователя. Твоя работа:

1) elementId — какой именно элемент раздела имеет в виду эксперт.
   Используй подсказки-референсы (после ↳). Если заметка про несколько
   элементов сразу или общая — ставь "generalCondition". Если эксперт явно
   указывает другой элемент, чем «текущий выбранный» — меняй elementId на
   тот, о котором он говорит. Поле обязательное.

2) Внимательно вычитай заметку и выпиши КАЖДОЕ упомянутое повреждение,
   дефект или замечание отдельным тегом. Не объединяй разные дефекты в
   один тег. Синонимы считай повреждениями (например: «притёртость»,
   «потёртость», «затир», «след контакта» = потёртость; «царапка» = царапина;
   «вмятинка» = вмятина; «ржа», «коррозия» = ржавчина; «крашена»,
   «перекрашен» = покраска).
   Для каждого повреждения:
   • сначала ищи подходящий тег в каталоге раздела (точное совпадение
     по смыслу, не по буквам — учитывай синонимы и формы слова);
   • если такого тега в каталоге нет — придумай короткое имя (1–3 слова,
     по-русски, с заглавной буквы, без точек) и добавь его в нужный массив.
   Если эксперт прямо говорит, что повреждений нет («целое», «без дефектов»,
   «всё ок») — оставляй оба массива пустыми и ставь noDamage=true.
   В остальных случаях noDamage=false и хотя бы один тег обязан быть.

3) Для каждого тега реши, серьёзный это дефект или нет:
   • серьёзные: коррозия сквозная, перекрас, замена силового элемента,
     следы ДТП, треснутый/деформированный силовой элемент, разрушение узла;
   • не серьёзные: сколы, царапины, локальные подкрасы, потёртости,
     притёртости, грязь, мелкие вмятины без повреждения ЛКП.

4) Переформулируй заметку 1–2 короткими предложениями делового тона:
   что именно, где на элементе, насколько критично. Сохрани ВСЕ факты
   из оригинала, ничего не додумывай и не выбрасывай. Поле note не должно
   быть пустым, если заметка эксперта не пустая.
${existingNote?.trim() ? `\n5) ВАЖНО: к этому элементу уже есть СОХРАНЁННАЯ заметка эксперта:\n"""\n${existingNote.trim()}\n"""\nНовая заметка — это ДОПОЛНЕНИЕ, а не замена. В поле note верни ОБЪЕДИНЁННУЮ заметку: сохрани ВСЕ факты из старой заметки и добавь новые из текущей. Не выбрасывай детали старой заметки, даже если новая их не упоминает. Если старая и новая описывают одно и то же — объедини без повторов. Теги тоже объединяй: упомянутые в старой заметке повреждения по-прежнему актуальны, добавляй к ним новые из текущей.\n` : ""}

Элементы раздела (используй ИМЕННО эти id):
${elList}

Каталог тегов раздела:
${tagList}

Структура ответа — ВСЕ ПЯТЬ полей ОБЯЗАТЕЛЬНЫ всегда, даже если массивы пустые:
{
  "elementId": "<id из списка>",
  "noDamage": true|false,
  "seriousTags": ["имя_тега", ...],
  "nonSeriousTags": ["имя_тега", ...],
  "note": "переформулированная заметка"
}
Никогда не возвращай пустой объект {}. Если сомневаешься в elementId —
ставь "generalCondition" и всё равно заполняй остальные поля по тексту заметки.


`;
};


/**
 * Роутер заметок осмотра: по свободному тексту эксперта определяем,
 * к какому разделу/элементу осмотра относится сообщение. Если смысла нет —
 * возвращаем noMatch=true и короткое пояснение.
 */
export const CLICHE_INSPECTION_ROUTE = (
  sections: Array<{
    snake: string;
    label: string;
    elements: Array<{ id: string; label: string }>;
  }>,
  current: { sectionSnake: string; sectionLabel: string; elementId: string; elementLabel: string },
) => {
  const sectionsBlock = sections
    .map((s) => {
      const els = s.elements
        .map((el) => `      • ${el.id} — ${el.label}`)
        .join("\n");
      return `  • ${s.snake} — ${s.label}\n${els}`;
    })
    .join("\n");

  return `Ты — маршрутизатор заметок технического осмотра автомобиля.
Тебе приходит свободный текст эксперта. Твоя единственная задача — понять,
к КАКОМУ разделу осмотра и КАКОМУ элементу внутри раздела относится эта реплика.
Сами повреждения и теги извлекать НЕ нужно — этим займётся отдельный шаг.

Текущий выбранный раздел: «${current.sectionLabel}» (snake: ${current.sectionSnake}).
Текущий выбранный элемент: «${current.elementLabel}» (id: ${current.elementId}).
Если эксперт явно говорит про другой раздел/элемент — выбирай его. Если в
тексте нет явных указаний и он осмысленно продолжает текущий контекст
осмотра — оставайся в текущем разделе/элементе.

Разделы и элементы (выбирай ИМЕННО эти snake и id):
${sectionsBlock}

Верни ТОЛЬКО валидный JSON без markdown и пояснений:
{
  "section": "<snake одного из разделов или null>",
  "elementId": "<id элемента из выбранного раздела или null, или \\"generalCondition\\">",
  "confidence": <число 0..1 — насколько уверен, что это про осмотр>,
  "noMatch": <true если текст вообще не про осмотр автомобиля
              или это бессмысленный/неразборчивый набор слов>,
  "bulkGeneralCondition": <true если эксперт ЯВНО говорит, что все элементы
              раздела (или все фото раздела) надо считать общими / отнести
              к «общему состоянию». Примеры: «все элементы общие»,
              «все фото общие», «весь раздел общим состоянием», «всё в общее»,
              «общий осмотр кузова», «по разделу — общее состояние».
              Иначе false>,
  "reason": "очень короткое пояснение (1 фраза)"
}

Правила:
- noMatch=true ставь когда: текст бессмыслен, это случайный набор букв,
  это вопрос не по осмотру (погода, политика и т.п.), это команда не про
  раздел осмотра. В этом случае section=null, elementId=null, confidence=0,
  bulkGeneralCondition=false.
- Если текст ОТНОСИТСЯ к осмотру, но раздел не очевиден — выбирай тот,
  к которому он подходит лучше всего, и снижай confidence (например 0.4).
- bulkGeneralCondition=true ставь, только если эксперт явно просит сделать
  ВСЕ элементы/фото раздела общими. Одиночная заметка «общее состояние
  нормальное» — это elementId="generalCondition", но bulkGeneralCondition=false.
- Не комментируй ничего сверх JSON.


`;
};




export const CLICHE_TEST_DRIVE = `${COMMON}

Извлеки данные тест-драйва из текста эксперта. Поля (все опциональные —
включай только то, что упомянуто):
- testDriveIsIncluded: false если эксперт сказал, что тест-драйв НЕ проводился, иначе true.
- testDriveEngineIsWorkingProperly: true/false по двигателю в движении.
- testDriveTransmissionIsWorkingProperly: true/false по КПП.
- testDriveSteeringWheelIsWorkingProperly: true/false по рулевому.
- testDriveSuspensionInDriveIsWorkingProperly: true/false по подвеске.
- testDriveBrakesInDriveIsWorkingProperly: true/false по тормозам.
- testDriveEngineTags / testDriveTransmissionTags / testDriveSteeringWheelTags /
  testDriveSuspensionInDriveTags / testDriveBrakesInDriveTags: массивы коротких
  меток с конкретными замечаниями (например ["вибрация на 80","пинок 2-3"]).
- testDriveNote: общая заметка по тест-драйву (1–3 предложения).


`;

export const CLICHE_RESULT = `${COMMON}

Эксперт диктует итог осмотра. Раздели его на два поля:
- summaryInspectionNote: краткое резюме текущего состояния авто
  (что нашли, ключевые дефекты, общее впечатление).
- resultSpecialistNote: вердикт/рекомендация специалиста
  (брать/торговаться/отказаться/рекомендовать и т.п.).

Если эксперт сказал только одно — заполни только соответствующее поле.

Верни ТОЛЬКО валидный JSON с этими двумя полями (любое можно опустить).


`;

/** Parse a strict-JSON model response, tolerating ```json fences. */
export function parseJsonResponse<T = unknown>(content: string | null | undefined): T | null {
  if (!content) return null;
  let s = content.trim();
  // strip ```json ... ``` fence
  const fence = s.match(/^```(?:json)?\s*([\s\S]*?)```$/i);
  if (fence) s = fence[1].trim();
  // grab first {...} block as fallback
  if (!s.startsWith("{") && !s.startsWith("[")) {
    const m = s.match(/[{[][\s\S]*[}\]]/);
    if (m) s = m[0];
  }
  try {
    return JSON.parse(s) as T;
  } catch {
    return null;
  }
}

const ENGINE_TYPES = ["Бензин", "Дизель", "Гибрид", "Электро", "Газ/Бензин"] as const;
const TRANSMISSIONS = ["АКПП", "МКПП", "Робот", "Вариатор"] as const;
const DRIVE_TYPES = ["Передний", "Задний", "Полный"] as const;

export function pickEnum<T extends readonly string[]>(
  raw: unknown,
  allowed: T,
): T[number] | undefined {
  if (typeof raw !== "string") return undefined;
  const s = raw.trim().toLowerCase().replace(/ё/g, "е");
  for (const v of allowed) {
    if (v.toLowerCase().replace(/ё/g, "е") === s) return v;
  }
  // fuzzy aliases
  if (/бенз/.test(s)) return allowed.includes("Бензин" as T[number]) ? ("Бензин" as T[number]) : undefined;
  if (/диз/.test(s)) return allowed.includes("Дизель" as T[number]) ? ("Дизель" as T[number]) : undefined;
  if (/гибр/.test(s)) return allowed.includes("Гибрид" as T[number]) ? ("Гибрид" as T[number]) : undefined;
  if (/электр/.test(s)) return allowed.includes("Электро" as T[number]) ? ("Электро" as T[number]) : undefined;
  if (/(акпп|автомат)/.test(s)) return allowed.includes("АКПП" as T[number]) ? ("АКПП" as T[number]) : undefined;
  if (/(мкпп|механик)/.test(s)) return allowed.includes("МКПП" as T[number]) ? ("МКПП" as T[number]) : undefined;
  if (/робот/.test(s)) return allowed.includes("Робот" as T[number]) ? ("Робот" as T[number]) : undefined;
  if (/вариатор|cvt/.test(s)) return allowed.includes("Вариатор" as T[number]) ? ("Вариатор" as T[number]) : undefined;
  if (/передн/.test(s)) return allowed.includes("Передний" as T[number]) ? ("Передний" as T[number]) : undefined;
  if (/задн/.test(s)) return allowed.includes("Задний" as T[number]) ? ("Задний" as T[number]) : undefined;
  if (/полн|4wd|awd|4x4/.test(s)) return allowed.includes("Полный" as T[number]) ? ("Полный" as T[number]) : undefined;
  return undefined;
}

export { ENGINE_TYPES, TRANSMISSIONS, DRIVE_TYPES };

/**
 * Из веб-контекста (выдачи поиска) определи каноническое имя автобренда.
 * Используется когда каталог не нашёл совпадений по подсказке эксперта
 * (например, эксперт написал «VW» → каноническое имя «Volkswagen»).
 */
export const CLICHE_CANONICAL_BRAND = (
  hint: string,
  webContext: string,
) => `${COMMON}

Из текста ниже определи каноническое (полное) название автомобильного бренда.
Это нужно чтобы потом найти бренд в справочнике carreports по точному имени.

Подсказка эксперта: ${JSON.stringify(hint)}

Веб-контекст:
${webContext}

Верни ТОЛЬКО JSON:
{
  "brandName": "<каноническое имя бренда, например Volkswagen, BMW, Lada>",
  "confidence": <0..1>
}


`;

/**
 * Уточняющий шаг: эксперт назвал только модель («тигуан», «camry», «x5»),
 * а бренд не указал. Просим ИИ определить бренд по имени модели.
 */
export const CLICHE_INFER_BRAND_FROM_MODEL = (
  modelName: string,
  userText: string,
  webContext?: string,
) => `${COMMON}

Эксперт назвал только модель автомобиля без указания марки. Определи марку
(производителя) по имени модели. Это нужно, чтобы дальше найти марку в
каталоге carreports.

Имя модели как назвал эксперт: ${JSON.stringify(modelName)}
Исходный текст эксперта: ${JSON.stringify(userText)}
${webContext ? `\nКонтекст из веб-поиска:\n${webContext}\n` : ""}

Примеры:
- «тигуан»/«tiguan» → Volkswagen
- «камри»/«camry» → Toyota
- «x5»/«икс пять» → BMW
- «логан»/«logan» → Renault

Верни ТОЛЬКО JSON:
{
  "brandName": "<каноническое имя марки, например Volkswagen, Toyota, BMW>",
  "modelCarName": "<нормализованное имя модели, например Tiguan, Camry, X5>",
  "confidence": <0..1>,
  "needsWeb": <true если уверенности нет и стоит уточнить веб-поиском>
}


`;

/**
 * Свободный режим вопросов. ИИ отвечает кратко и по делу, опираясь
 * на текущее заполнение черновика — НЕ извлекает данные и НЕ изменяет шаг.
 */
export const CLICHE_ASK = (
  stepLabel: string,
  draftContext: string,
) => `Ты — ассистент-эксперт по техническому осмотру автомобилей.
Эксперт сейчас находится на шаге «${stepLabel}» и задаёт уточняющий вопрос.

Правила ответа:
1. Отвечай ТОЛЬКО в контексте текущего шага «${stepLabel}». Если вопрос про другой шаг — мягко скажи, что это будет на соответствующем шаге, и кратко поясни.
2. Опирайся на «Состояние шага» ниже: что уже заполнено, что осталось. Если эксперт спрашивает «что осталось / что заполнено / что дальше» — отвечай конкретно из состояния.
3. Не возвращай JSON, не извлекай поля, не меняй черновик — это режим диалога.
4. Будь кратким (2–5 предложений), по делу, на «вы», без воды.
5. Если вопрос про дефект/норматив/методику — дай практичный экспертный ответ по этому шагу.

Состояние шага «${stepLabel}»:
${draftContext || "(черновик пуст)"}

Вопрос эксперта:
`;


/**
 * Переформулировать пользовательскую заметку с учётом уже зафиксированных
 * тегов/контекста. AI должен сохранить смысл и факты, не дублировать словами
 * то, что уже есть в тегах, и вернуть нейтрально‑деловой текст для отчёта.
 */
export const CLICHE_REFORMULATE_NOTE = (
  stepLabel: string,
  scopeLabel: string,
  tagNames: string[],
  existingNote: string,
) => `
Ты — редактор отчёта по осмотру авто.
Шаг: ${stepLabel}.
Контекст: ${scopeLabel}.

Зафиксированные пользователем теги (замечания/неполадки):
${tagNames.length ? tagNames.map((n) => `- ${n}`).join("\n") : "(тегов нет)"}

Исходная заметка эксперта:
"""
${existingNote}
"""

Требования к переформулировке:
- Сохрани все факты и числа из исходной заметки.
- ОБЯЗАТЕЛЬНО учитывай теги выше: если в заметке они не отражены, кратко
  упомяни их (одним словосочетанием на тег) как зафиксированные замечания,
  чтобы итоговый текст согласовывался со списком тегов и не противоречил ему.
- Если тег уже явно описан в исходной заметке — не дублируй его отдельно.
- Пиши нейтрально‑деловым языком отчёта, без оценок и эмоций.
- Короткие предложения, без вводных слов и канцелярита.
- Если исходная заметка пустая И тегов нет — верни её без изменений.

Ответ — СТРОГО валидный JSON одним объектом:
{ "note": "<переформулированная заметка>" }
Никаких пояснений вне JSON.
`;


/**
 * Нормализация пользовательских тегов: исправление опечаток, сокращение
 * длинных формулировок и классификация по тяжести (serious / non_serious).
 */
export const CLICHE_NORMALIZE_TAGS = (
  categoryLabel: string,
  rawNames: string[],
) => `
Ты — редактор тегов в отчёте по осмотру авто.
Категория: ${categoryLabel}.

Исходные теги от пользователя (могут быть с опечатками или слишком длинные):
${rawNames.map((n, i) => `${i + 1}. ${n}`).join("\n")}

Задача для каждого тега:
1) Нормализуй текст:
   - Исправь опечатки и грамматику.
   - Если формулировка длинная (больше 3-4 слов) — замени коротким синонимом
     или техническим словосочетанием (2-3 слова), сохранив смысл.
   - Если формулировка уже короткая и корректная — оставь как есть.
   - Не объединяй разные теги в один и не разбивай один тег на несколько.
   - Используй нейтрально-технический язык отчёта по авто.
2) Классифицируй неполадку:
   - "serious" — критическая или существенно влияющая на безопасность /
     ресурс узла неисправность (например: течь масла, стук в двигателе,
     неисправность тормозов, люфт рулевой рейки).
   - "non_serious" — мелкое замечание, не критичное, требующее внимания
     (например: лёгкий гул подшипника, незначительный люфт, скрип).
   - Тип ОБЯЗАТЕЛЕН для каждого тега, null недопустим. Если сомневаешься —
     выбирай "non_serious".

Ответ — СТРОГО валидный JSON одним объектом, без markdown и пояснений:
{ "tags": [ { "name": "<нормализованный тег 1>", "type": "serious" | "non_serious" }, ... ] }
Длина массива tags ДОЛЖНА совпадать с числом исходных тегов и порядок сохраняется.
`;


